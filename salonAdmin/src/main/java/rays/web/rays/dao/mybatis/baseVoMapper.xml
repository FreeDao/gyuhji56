<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper 
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rays.web.rays.dao.mybatis.WebScanDao">

	<select id="selectSourceDetectedById" parameterType="int"
		resultType="SourceDetectVo">
		select
		*
		from webscan_source_detect where id=#{varRuleId}
	</select>
	<select id="selectSourceDetectedList" 
		resultType="SourceDetectVo">
		select
		*
		from webscan_source_detect where audit_status = 0 order by level_3_count desc,level_2_count desc,level_2_count desc limit #{offset}, #{len}
	</select>
	<select id="selectTotalUnAudit" 
		resultType="int">
		select
		count(*)
		from webscan_source_detect where audit_status = 0 
	</select>

</mapper> 