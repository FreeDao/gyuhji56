<?xml version="1.0" encoding="UTF-8" ?>     
<!DOCTYPE configuration     
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"     
    "http://mybatis.org/dtd/mybatis-3-config.dtd">
    
<configuration>
	<properties>
	  <property name="jdbc.ip" value="127.0.0.1"/>
	  <property name="jdbc.port" value="3306"/>
	  <property name="jdbc.dbname" value="rl2ee"/>
	  <property name="jdbc.username" value="root"/>
	  <property name="jdbc.password" value="123456"/>
	</properties>
	
	<settings>
		<!--对在此配置文件下的所有cache 进行全局性开/关设置。 default="false" -->
		<setting name="cacheEnabled" value="true" />
		<!--全局性设置懒加载。如果设为‘false’，则所有相关联的都会被初始化加载。 default="true" -->
		<setting name="lazyLoadingEnabled" value="false" />
		<!--允许和不允许单条语句返回多个数据集（取决于驱动需求） default="true" -->
		<setting name="multipleResultSetsEnabled" value="true" />
		<!--使用列标签代替列名称。不同的驱动器有不同的作法。参考一下驱动器文档，或者用这两个不同的选项进行测试一下。 default="true" -->
		<setting name="useColumnLabel" value="true" />
		<!--允许JDBC 生成主键。需要驱动器支持。如果设为了true，这个设置将强制使用被生成的主键，有一些驱动器不兼容不过仍然可以执行。 default="false" -->
		<setting name="useGeneratedKeys" value="true" />
		<!--配置和设定执行器，SIMPLE 执行器执行其它语句。REUSE 执行器可能重复使用prepared statements 语句，BATCH执行器可以重复执行语句和批量更新。 
			default="SIMPLE" -->
		<setting name="defaultExecutorType" value="SIMPLE" />
		<!--设置一个时限，以决定让驱动器等待数据库回应的多长时间为超时 default="Not Set" -->
	</settings>

	<!--设置别名 -->
	<typeAliases>
		<typeAlias alias="SourceDetectVo" type="rays.web.rays.vo.SourceDetectVo" />
		<typeAlias alias="HostDetectedVo" type="rays.web.rays.vo.HostDetectedVo" />
		<typeAlias alias="PageDetectedVo" type="rays.web.rays.vo.PageDetectedVo" />
	</typeAliases>


	<environments default="development">
		<environment id="dataSource">
			<transactionManager type="jdbc" />
			<dataSource type="POOLED">
				<property name="driver" value="com.mysql.jdbc.Driver" />
		        <property name="url" value="jdbc:mysql://${jdbc.ip}:${jdbc.port}/${jdbc.dbname}?useUnicode=true&amp;characterEncoding=utf8" />  
		        <property name="username" value="${jdbc.username}" />  
		        <property name="password" value="${jdbc.password}" />  
			</dataSource>
		</environment>
	</environments>
	
	<mappers>
		<mapper resource="rays/web/rays/dao/mybatis/baseVoMapper.xml" />
	</mappers>
</configuration> 